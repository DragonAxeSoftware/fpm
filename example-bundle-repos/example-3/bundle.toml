# Example leaf bundle (no dependencies)
# This bundle contains base styles and CSS variables

fpm_version = "0.1.0"
identifier = "fpm-bundle"
version = "1.0.0"
description = "Base styles and CSS variables - foundation for other bundles"

[bundles]
# No dependencies - this is a leaf bundle
